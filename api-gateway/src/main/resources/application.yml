server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    kubernetes:
      discovery:
        enabled: true
        all-namespaces: false
      config:
        enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true

logging:
  level:
    uz.coder: DEBUG
    org.springframework.cloud.gateway: INFO